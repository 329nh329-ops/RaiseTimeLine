# Git / GitHub運用ルール

このプロジェクトでは、以下のルールを必ず守って開発を進める。

## 基本フロー

1. 作業を始める前に、GitHub Issueを作成する（機能追加・バグ修正・ドキュメント更新など、まとまった作業単位ごとに1つ）
2. Issueに対応する作業ブランチを作成する
3. 作業ブランチ上でコミットを積む
4. 実装後は品質チェック（Lint・型チェック・テスト・ビルド）を実行する
5. 作業が完了したらPull Requestを作成する
6. 内容を確認したうえで、PRをマージする

## ブランチ運用

- **mainブランチへの直接pushは禁止**。すべての変更はブランチを切ってPR経由でmainに取り込む
- ブランチ名は `feature/{Issue番号}-{短い説明（英語・ハイフン区切り）}` の形式にする
  - 例：Issue #5「投稿の編集・削除機能を追加する」→ `feature/5-post-edit-delete`
- ブランチはIssueと1対1で対応させる。1つのブランチで複数のIssueを同時に扱わない

## Issue運用

- 作業に着手する前に、その作業内容をGitHub Issueとして登録する
- Issueには背景・目的・実装内容・完了条件を記載する
- Issueのタイトルは、何を実現する作業なのかが一目でわかる短い文にする
- Issue番号は、対応するブランチ名・コミットメッセージ・PRの説明で参照し、作業の追跡ができるようにする

## Pull Request運用

- Pull Requestは対応するIssueへの参照を含める（例：本文に `Closes #5` と書き、マージ時にIssueが自動クローズされるようにする）
- PRには変更内容・変更理由・品質チェック結果を記載する
- マージは内容を確認したうえで行う
- CIを整備した後は、CIが成功していることをマージの前提条件とする

## GitHub側の保護設定

- mainブランチには GitHub の Branch Protection Rule を設定し、直接pushを禁止してPR経由のマージのみを許可する
- CI整備後は、ステータスチェックの必須化を検討する

## AIへの依頼に関するルール

- 実装前に必ず現状調査・影響範囲の調査を行い、方針を提案する。ユーザーの承認を得るまで実装に着手しない
- 大きな機能は Issue を分割し、1つの変更範囲を小さく保つ
- 既存の設計・既存コードを無断で変更しない
- PR作成後は自動でマージしない。マージはユーザーの承認を得てから行う
- AWSのAccess Key・Secret Access Key・パスワード等の秘密情報をGit・チャットに貼り付けない

## 関連ドキュメント

- [要件定義.md](./要件定義.md)：プロジェクト全体の要件
- [docs/機能一覧.md](./docs/機能一覧.md)、[docs/機能定義/](./docs/機能定義/)：機能ごとの詳細仕様
- [docs/画面設計.md](./docs/画面設計.md)：画面一覧・画面遷移
- [docs/データベース設計.md](./docs/データベース設計.md)：ER図・テーブル定義
- [docs/技術スタック.md](./docs/技術スタック.md)：使用技術
- [docs/インフラ構成.md](./docs/インフラ構成.md)：AWS構成（EC2 + RDS + ALB前提）
